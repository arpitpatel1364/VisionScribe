# models/

Place YOLO model weights here.

Default: `yolov8n.pt` auto-downloads on first run (no action needed).

For better document layout detection, download a fine-tuned model
and set `YOLO_MODEL_PATH=./models/your_model.pt` in `.env`.

This directory is gitignored.
